Reprendre l'exercice précédent, installer sequelize et configurer sequelize et sqlite3. 
Ajouter une base de données SQL en local et modifier l'API pour qu'elle utilise la base de données au lieu du tableau tasks, ou du fichier json préalablement configuré. 

On devra ajouter un model Task, modifier le fonctionnement du service, et ajouter une configuration à sequelize pour qu'il utilise la base de données. 